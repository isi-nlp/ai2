defaults:
  - model: roberta-large-scratch
  - task: alphanli_variation_1
classifier_dropout: 0.3
eval_after_training: True
max_epochs: 8
random_seed: 42
progress_bar_refresh_rate: 1
save_top_N_models: 1 # -1 if you want all models saved
